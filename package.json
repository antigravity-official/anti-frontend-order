{
  "name": "anti-frontend-order",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --setupFiles dotenv/config",
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "server": "node ./mock-server/index.js",
    "lint": "gts lint",
    "lint:ci": "eslint '**/*.ts' -f checkstyle -o checkstyle-result.xml",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "format": "prettier --write **/*.ts",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^28.1.6",
    "dotenv": "^16.0.3",
    "husky": "^8.0.1",
    "jest": "^28.1.3",
    "module-alias": "^2.2.2",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-jest": "^28.0.7"
  },
  "_moduleAliases": {
    "@adapters": "./src/adapters",
    "@domains": "./src/domains",
    "@frameworks": "./src/frameworks",
    "@hooks": "./src/frameworks/web/hooks",
    "@di": "./src/frameworks/web/di/index.ts"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "eslint-plugin-prettier": "^4.2.1",
    "gts": "^3.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  }
}
