import styled from 'styled-components';

export const OrdersSection = styled.section`
  width: 80%;
  margin: 0 auto;
  padding: 80px 0 0 0;
`;
export const Orders_loadingText = styled.div`
  color: ${({ theme }) => theme.color.DARK};
  font-size: 30px;
  font-weight: 700;
`;
